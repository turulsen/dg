{
  "name": "dg-campaign-functions",
  "private": true,
  "description": "Cloud Functions for the Firebase migration: the auth bridge (exchangeAgentToken, handlerLogin) plus the mirror-as-a-schedule option for Phase 1.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.1",
    "googleapis": "^144.0.0"
  }
}
